#include <AVCore/AVCore.h>
int main() {
  // AudioVideoSynchronizer AVSync;
  // AVSync.SetSpeedRate(2.0);
  // std::this_thread::sleep_for(std::chrono::milliseconds(100));
  // LOGINFO("1. pts = %llu", AVSync.GetSyncPts()); // 0
  // AVSync.Resume();
  // std::this_thread::sleep_for(std::chrono::milliseconds(100));
  // LOGINFO("2. pts = %llu", AVSync.GetSyncPts()); // 100
  // AVSync.Suspend();
  // std::this_thread::sleep_for(std::chrono::milliseconds(100));
  // LOGINFO("3. pts = %llu", AVSync.GetSyncPts()); // 100
  // AVSync.Resume();
  // std::this_thread::sleep_for(std::chrono::milliseconds(100));
  // LOGINFO("4. pts = %llu", AVSync.GetSyncPts()); // 200
  // AVSync.Clear();
  // std::this_thread::sleep_for(std::chrono::milliseconds(100));
  // LOGINFO("5. pts = %llu", AVSync.GetSyncPts()); // 0
  // AVSync.Resume();
  // std::this_thread::sleep_for(std::chrono::milliseconds(100));
  // LOGINFO("6. pts = %llu", AVSync.GetSyncPts()); // 100A
  return 0;
}